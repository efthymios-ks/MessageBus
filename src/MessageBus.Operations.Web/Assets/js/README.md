# Static JS lives in `wwwroot/js/`

`Assets/` is for things that need building (SCSS → CSS). Client-side JavaScript is not built —
it's served directly from `wwwroot/js/` by ASP.NET Core's static files middleware. Files that
were here have moved to `wwwroot/js/`.
